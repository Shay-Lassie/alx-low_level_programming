#include "main.h"

/**
 * _puts - print string followed by a newline
 * Description: performs the same function as puts
 * @str: the string
 * Return: str
 */

void _puts(char *str)
{
	printf("%d\n", *str);

	return (str);
}
