#include <stdio.h>

/**
 * reset_to_98 - funtion takes an int pointer as parameter
 * swap_int: updates the pointed value to 98
 * int _strlen: returns string length
 * Return: Always 0
 */

void reset_to_98(int *n);	/* function declaration */

void swap_int(int *a, int *b);	/* swap integer values */

int _strlen(char *s);	/* string length */

void _puts(char *str);
