#include <stdio.h>

/*
 * To print using the printf function
 */

int main(void)
	/*
	 * The main function is the ntry point
	 */
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
