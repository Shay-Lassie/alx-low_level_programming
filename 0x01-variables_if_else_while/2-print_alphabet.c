#include <stdio.h>

/**
 * main- print the alphabet in lowercase
 * Description: loop through the alphabet and print in LC
 * Return: 0
 */

int main (void){
	char a;
	for (a = 'a'; a <= 'z'; ++a)
		putchar(a);
	return(0);
}
