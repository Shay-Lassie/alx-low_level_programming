#include "main.h"

/**
 * main - print name despite change
 * Description: add new line after
 * @argc: number of aarguments
 * @argv: array of passed argumrnys in string format
 * Return: Always 0
 */

int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
